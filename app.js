(()=>{const q=(s,r=document)=>r.querySelector(s),qa=(s,r=document)=>[...r.querySelectorAll(s)];const body=document.body;const menuBtn=q('[data-menu-btn]');const mobile=q('[data-mobile-panel]');if(menuBtn&&mobile){menuBtn.addEventListener('click',()=>{const open=body.classList.toggle('menu-open');menuBtn.setAttribute('aria-expanded',String(open));mobile.setAttribute('aria-hidden',String(!open))});qa('[data-mobile-panel] a').forEach(a=>a.addEventListener('click',()=>{body.classList.remove('menu-open');menuBtn.setAttribute('aria-expanded','false');mobile.setAttribute('aria-hidden','true')}))}const more=q('[data-more-wrap]'),moreBtn=q('[data-more-btn]');if(more&&moreBtn){moreBtn.addEventListener('click',e=>{e.stopPropagation();more.classList.toggle('open')});document.addEventListener('click',()=>more.classList.remove('open'));more.addEventListener('click',e=>e.stopPropagation())}const progress=q('[data-progress]');const setProgress=()=>{if(!progress)return;const max=document.documentElement.scrollHeight-innerHeight;progress.style.width=`${max>0?(scrollY/max)*100:0}%`};addEventListener('scroll',setProgress,{passive:true});setProgress();const glow=q('[data-cursor-glow]');if(glow&&matchMedia('(pointer:fine)').matches){addEventListener('pointermove',e=>{glow.style.left=e.clientX+'px';glow.style.top=e.clientY+'px'})}const observer=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting){e.target.classList.add('visible');observer.unobserve(e.target)}}),{threshold:.12});qa('.reveal').forEach(el=>observer.observe(el));if(matchMedia('(pointer:fine)').matches&&!matchMedia('(prefers-reduced-motion: reduce)').matches){qa('[data-tilt]').forEach(card=>{card.addEventListener('pointermove',e=>{const r=card.getBoundingClientRect();const x=(e.clientX-r.left)/r.width-.5;const y=(e.clientY-r.top)/r.height-.5;card.style.transform=`perspective(900px) rotateX(${(-y*5).toFixed(2)}deg) rotateY(${(x*7).toFixed(2)}deg) translateY(-4px)`});card.addEventListener('pointerleave',()=>card.style.transform='')})}qa('[data-filter]').forEach(btn=>btn.addEventListener('click',()=>{const group=btn.closest('[data-filter-group]');if(!group)return;qa('[data-filter]',group).forEach(b=>b.classList.remove('active'));btn.classList.add('active');const target=btn.dataset.filter;qa('[data-category]').forEach(card=>{const match=target==='all'||card.dataset.category===target;card.hidden=!match})}));})();